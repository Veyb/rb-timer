{
  "collectionName": "components_catalog_stat_modifiers",
  "info": {
    "displayName": "Stat modifier",
    "icon": "chartCircle",
    "description": "A signed adjustment to one combat statistic. Only pDef appears in the source so far, which is exactly why this is a list rather than a column per statistic: the vocabulary matches the stats component so a new entry needs no reshaping."
  },
  "options": {},
  "attributes": {
    "stat": {
      "type": "enumeration",
      "enum": ["hp", "mp", "pAtk", "mAtk", "pDef", "mDef", "acc", "eva"],
      "required": true
    },
    "value": {
      "type": "integer",
      "required": true
    }
  }
}

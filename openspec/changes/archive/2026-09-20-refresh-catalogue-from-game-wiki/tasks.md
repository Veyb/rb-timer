## 1. Reaching the source

- [x] 1.1 Add a fetch helper that clears the interstitial by running the page's own script in a `vm` sandbox and replaying the captured request; verify it returns the article at over 5 MB with the title `Lu4: B-W-C Wiki: Raid Bosses`, in three requests and without a browser
- [x] 1.2 Make the sandbox omit the globals the script probes for (`Buffer`, `webdriver`, `_phantom`, `callPhantom`, `__phantomas`, `emit`, `spawn`, `domAutomation`) and report a viewport different from the screen size; verify the captured request carries no `I84f…` headless flag header
- [x] 1.3 Treat a response that still looks like the interstitial after the POST as an error rather than an empty page; verify a forced failure exits non-zero and writes nothing
- [x] 1.4 Cache the fetched HTML to disk and reuse it when asked, so parser work needs no further requests; verify a second run with the cache flag makes no network call
- [x] 1.5 Put a delay between requests in the per-boss passes, and document in the script header that `robots.txt` disallows crawling and these commands are run by hand

## 2. The drop refresh (one request)

- [x] 2.1 Parse the article into boss records keyed on `/npc/<id>-<slug>` rather than on the badge text; verify 158 bosses are found, since the badge reads `Raid Boss`, `Subclass Raid Boss` or a flavour title and cannot be used to select them
- [x] 2.2 Read `epic` from the `Epic Bosses` section and `subclass` from the `Subclass` badge; verify 3 and 4 respectively, and that both agree with what `data-wiki.ts` already holds
- [x] 2.3 Parse both `<details>` drop tables per boss, selecting by the summary text and not by position — `Drop Gamma` comes first; verify 3761 rows for `Drop Black/ White/ Carmine` and 3744 for `Drop Gamma`
- [x] 2.4 Capture per drop row: item id, slug, icon key, name, grade, quantity range, chance to four decimals, and the `Group Chance` the row falls under. Fold the gate into the chance rather than storing the group: the source states a weight inside a gate, and their product is the rate. Verified against the original scrape, which published the folded value — `Puma Skin Gaiters` is 38.7833 inside a 97.4288% gate, which is the 37.7861 that scrape recorded, and folding raises agreement with it from 2047 to 2273 of 2651 shared rows
- [x] 2.5 Write the B-W-C tables into `data-wiki.ts`, edited in place so the fields this page cannot see survive, and the Gamma tables into a new `data-wiki-gamma.ts`. Not in `RaidBossInfo` shape: measured across a sample, the servers disagree on level, on every statistic and on the soul-crystal level, so a full copy would either cost 158 more requests or pass our numbers off as theirs. `GammaBossInfo` holds only what the article states for Gamma — level, eight statistics and the drops — and its header names what it omits. Verify both files parse through the reader's own `vm` path
- [x] 2.6 Report differences without applying them by default — renames, added rows, removed rows, changed chances — and verify the report against the measured baseline: 287 rows only on the page and 137 only in the file, both exact, and 205 renamed rows across 76 names rather than the 207/78 first measured. The two are Queen Ant's rings, whose name the exploring parser could not read: it looked for `item-name__class-1` and theirs is `class-3`. They match by name now and are not renames
- [x] 2.7 Apply the differences behind an explicit flag, including the 33 icon keys that move from `_i02` to `_i00`; verify re-running reports no differences

- [x] 2.8 Add a shared icon step: for a set of icon keys, fetch `/i64/<key>.png` for each one with no file yet, convert it to the 64x64 webp the catalogue stores, and write it into a given directory. Verify it reports what it fetched, skips what is already there, and fails loudly rather than writing a truncated file
- [x] 2.9 Wire that step into the drop refresh so a `--write` run leaves no item icon missing; verify the 8 keys currently without a file — `accessary_phoenixs_ring_i02` and the rest — land in `mocks/raid-bosses/images/item-icons` at 64x64, and that a second run fetches nothing

## 3. The profile refresh (one request per boss)

- [x] 3.1 Fetch each boss from `/lu4-b-w-c/npc/<id>-<slug>` and never from the shared article or the `/lu4-gamma/` links it contains; verify Queen Ant reads level 56 and 10,176,400 HP rather than 75 and 24,462,500
- [x] 3.2 Parse level and race from `item-name__type` (`Bug, Lv. 56`); verify all 158 yield both, and report every boss whose race disagrees with the file — the article's icon hint is wrong for 14 of them and must not be used
- [x] 3.3 Parse the full statistics from `stat_line` rows, including `Accuracy / Evasion` which the article does not carry; verify all ten are present for every boss
- [x] 3.4 Parse respawn from `Respawn Time` (`10 hours ± 2 hours`) into the existing base-and-variance shape; verify the three scheduled epics still come through as fixed
- [x] 3.5 Parse `saMaxLevel` from the second `soul_crystal_step` in `soul_crystal_dropdown`; verify Eva's Guardian Millenu reads 11, and that the 44 bosses the file currently marks are covered Verified: 44 bosses carried one, 41 still do, and the three lost are exactly Core, Orfen and Queen Ant — the epics, which grant no crystal level on this server.
- [x] 3.6 Collect the skills each boss carries from `npc_skill_list`, recording group id, level and icon; verify the set of distinct skill groups is reported, and that the run states which of them carry modifiers
- [x] 3.7 Confirm on the full run whether server differences are confined to epic and subclass bosses; report every ordinary boss that disagrees, since the sample of four suggested none do Verified on 56 bosses whose pages exist for both servers: all 7 epic and subclass bosses differ, all 49 ordinary ones are identical in level, statistics and soul-crystal level.
- [x] 3.8 Give `RaidBossInfo` a `skills` field holding the keys of the skills a boss carries (`4416-12`), and have the profile refresh write it. Without it the pass collects skills and has nowhere to put them, and the seed — which reads the mock — would have nothing to relate. Verify every boss round-trips its keys through the reader's `vm` path

## 4. Skills, in the mock

Data only. The `skill` content type, the boss relation and the seed are in group 7,
so that nothing reaches the database until `RaidBossInfo` has settled.


- [x] 4.1 No per-group fetches. A boss's page carries, in the dropdown beside each skill, the description of the level *that boss* has — the racial trait's eight numbers for `4416-1`, the prose for `4273-3` — so every modifier is already in the 158 pages the profile pass cached. Skill pages state only the vocabulary of levels, which the reading client wants and the seed does not. Verified against Zombie Lord Ferkel: all eight of its skills, numbers and prose alike, come out of its own page
- [x] 4.2 Parse lines of the form `Label ±N` or `Label ±N%` into amounts, and read a named affinity — `Dagger/Rapier Resistance`, `Blunt Weapon Vulnerability` — into a direction with the skill's level as its amount. Neither kind may be dropped: checked against 88 stored affinities, 66 come from the racial trait's numbers and 22 exist only as named skills, so parsing numbers alone would lose a quarter of what the catalogue already holds. Verify the lore text duplicated onto `Misc. Lv. 19` and `Non-living Beings Lv. 20` still produces nothing Verified across every cached boss page: 25 skills, 99 modifiers, 0 subjects the vocabularies do not know.
- [x] 4.3 Decide what to keep by what a skill says, never by its group number. A dropdown yielding neither a `Label ±N` line nor a named affinity is not recorded — which drops the lore, the raid mechanics, and the stat-scale family `4408`-`4413` whose 17 carriers all sit at the neutral level and state nothing. Exclude `4415` explicitly: it is the weapon the boss swings, not a defence. An allowlist of groups was considered and rejected — the roster names 176 of them, and a patch moving one boss to `Extremely Weak P. Def. Lv. 1` and its −61% would be lost silently instead of arriving for free Verified: the lore, the raid mechanics and the whole `4408`-`4413` family fall out on their own, and `4415` is excluded by number.
- [x] 4.4 Sort each modifier into weapon, element, statistic or condition by its subject, folding `Crossbow` into `bow` and each `Dual X` into `X` — checked across the eighteen racial traits, a variant and its base appear together eight times and agree every time, so folding deduplicates rather than chooses. Fail the run on a subject in none of the vocabularies; verify the six elements match the existing enum exactly and that the nine conditions (`bleed`, `debuffs`, `derangement`, `hold`, `knockback`, `poison`, `shock`, `sleep`, `speed down`) are the full set Verified: 8 folding pairs across the racial traits, none disagreeing, and the elements match the existing enum exactly.
- [x] 4.5 Assert what is actually an error, not what merely looks like one. A subject in two units is normal — `fire` arrives as a percentage from the racial trait and as a level from a named skill, on seven bosses — so asserting against it would fail every run. What is worth a person's attention is a boss told opposite things about one subject in units that cannot be reconciled: one boss in 159, Gargoyle Lord Sirocco, whose Beast trait says vulnerable to daggers while its own skill says resistant. Report it by name, keep both, and let the reading client rank the personal skill over the racial one. Verified: 72 subjects are stated by more than one skill, 69 agree in direction, 3 do not, and 2 of those 3 share a unit and simply sum
- [x] 4.6 Fetch the skill icons the same way, into `mocks/raid-bosses/images/skill-icons`; verify every skill recorded has a file, and that a second run fetches nothing 23 skill icons fetched into `images/skill-icons`, all 64x64 webp; a second run fetches none.
- [x] 4.7 Add `mocks/raid-bosses/skills.ts` exporting `SKILLS: SkillInfo[]` — game id, level, name, icon key and modifiers — generated by the skill pass, and a `SkillInfo` type beside `RaidBossInfo`. Verify the keys written by task 3.8 all resolve against it and that no key resolves to a skill with no modifiers Verified: 25 skills, 267 boss-to-skill references, 0 dangling keys, 0 skills nothing carries, 0 skills without modifiers.

## 5. Removing what the source dropped

- [x] 5.1 Add `prune:catalog` reporting by default and deleting only behind a flag; verify a plain run lists candidates and deletes nothing The flag is `--delete` rather than the refreshes' `--write`: this is the one command here that takes something away, and it should read as that in a shell history.
- [x] 5.2 Refuse to run when the source yields no bosses; verify a deliberately emptied source exits non-zero with the catalogue untouched Checked before a single row is read, and verified with `deleting: true` so the refusal is what stops it rather than the report mode. Every count is unchanged afterwards; the runner turns the refusal into a non-zero exit the way every script here does.
- [x] 5.3 Remove drops the source no longer states, then items no drop points at, in that order; verify an item still dropped by any boss survives Verified, and the test had to be sharpened to mean anything: taking the slice's first drop removed `Proof of Loyalty`, which both of its bosses drop, so the item half never ran. It now picks a drop whose item nothing else in the slice names, and asserts every still-dropped item survives.
- [x] 5.4 Report rather than remove an item carrying a hand-set grade; verify one listed in `catalog-grades.json` is named in the report and left in place Verified against a grades file written into a temporary directory, the way the export test already does; `readCatalogGrades` takes a path for that reason. The drop goes, the item stays, and the item is named.
- [x] 5.5 Leave media alone; verify the file count is unchanged by a prune Asserted on both deleting runs. 210 backend tests pass.

## 6. Renaming the coordinates

- [x] 6.1 Rename `worldX`/`worldY` to `wikiX`/`wikiY` in `RaidBossInfo`, both data files, the reader, the seeder, the content type and the frontend type; verify `pnpm -r check-types` passes Done: `data.ts` held real world coordinates rather than pins and was deleted rather than renamed, so the rename covers the one data file that survived.
- [x] 6.2 Add the map refresh: parse `spawn-point` `left`/`top` from `/lu4-b-w-c/npc/spawn/<id>-<slug>` at full precision; verify Queen Ant reads `1557.9096045198 / 2527.9548022599` against the `1557.91 / 2527.95` the file rounds to today Verified, and read from `/lu4-gamma/` instead: pins are geography rather than a server parameter — four bosses carrying one on both servers agree bitwise — and this server omits the pin for 49 of the 158. All 158 sharpened, none moved.
- [x] 6.3 Fail the run if any boss reports more than one spawn point, rather than silently taking the first; verify the check fires on a crafted two-point response Also checks the map is still 3004px on every page: a pin is a pixel of one image, and a resize would silently reinterpret all 158.
- [x] 6.4 Fetch the map the pins are drawn on, which the catalogue does not hold. `wikiX`/`wikiY` are pixels of the source's 3004-pixel map and the map in the repository is a different 3072x4096 image, so without it the 158 coordinates refreshed above point at nothing. One request: the spawn pages name the file, so the pass reads the `src` beside the size it already checks. Store it at exactly 3004x3004 so a stored coordinate is literally a pixel of the stored image, and verify a second run fetches nothing Done in one request. The source serves it at 3004x3004 natively, so nothing was rescaled and a stored coordinate is a pixel of the stored file; 372 KB as webp. `world-map.ts` now exports both maps and says which coordinates belong to which, and the seed uploads both. A second run reports `map: already held` and fetches nothing. Also checks the map is still 3004px on every page: a pin is a pixel of one image, and a resize would silently reinterpret all 158.

## 7. The catalogue follows the mock

Everything that touches Strapi or the frontend. Skills arrive before the old
affinity path is retired, so there is a point where both describe the same
bosses and the new one can be checked against the old rather than after it.

- [x] 7.1 Add the `skill` content type with its modifier components, one component per kind so the subject is the field name; verify a weapon modifier cannot be stored with an element as its subject Verified: storing `fire` in a `skill.weapon-modifier` is refused by name, `spear` is accepted. One condition was renamed on the way in — `speed down` became `speedDown` — because every subject becomes an enumeration value, and it was the only one the source states with a space.
- [x] 7.2 Relate bosses to skills many-to-many and seed both, reading the keys from `skills.ts`; verify two bosses carrying the same skill name the same record, and that editing that record changes what both report Verified on a slice whose two bosses are now chosen for sharing an avatar *and* a skill — 35 of the 37 shared-avatar groups do, so the pair is found rather than hoped for. Rebalancing the shared record reaches both. The reader refuses a boss naming a skill `skills.ts` does not define, which is the same join check the drops already carry.
- [x] 7.3 Carry the skill's icon through the content type and the seed the way item icons already travel; verify a seeded skill returns its image Verified. The source states the icon as a key rather than a path, so the reader resolves it against `images/skill-icons`; the upload names it from the folder too, which is what keeps `skill4010.webp` from colliding with an item icon of the same name.
- [x] 7.4 Remove the `weapon-type` content type and the boss's `resistances` and `vulnerabilities` relations; verify the backend starts and `check-types` passes Verified: `check-types` clean, and the ten test files that boot a real Strapi all pass. The weapon-vocabulary invariant in `catalog-source.test.ts` was retired here rather than in 7.7 — it imported the deleted `WEAPON_TYPES`, so it was a compile error and not a red test.
- [x] 7.5 Remove the per-boss `elementModifiers` and `statModifiers`, whose values are now modifiers on a skill; verify the seed no longer writes them Verified by asserting on the content type itself rather than on a row: the boss carries `skills` and none of the four retired attributes. The two `catalog.*` modifier components went with them. `dist/` and `dist-test/` had to be cleared — Strapi builds into them and does not remove a schema that is gone from `src/`, so the type generation was still reporting the deleted components.
- [x] 7.6 Remove `weaponResistances`, `weaponVulnerabilities`, `elementModifiers` and `statModifiers` from `RaidBossInfo` and from the data file, now that `skills` carries what they said. Deliberately last: adding before removing leaves a point where both shapes exist and the new one can be checked to describe the same bosses, rather than finding out after the old one is gone. Verify the affinities derived from skills cover every boss that carried the old fields Checked before removing, and the check is why this ordering was worth it: every element and statistic modifier is reproduced exactly, and every weapon affinity on 157 of 158 bosses. The one exception, Evil Spirit Cyrion, is the old data contradicting itself — it and Nakondas carry the same Dragons trait and the old fields gave them opposite bow affinities, while five more Dragons carried none at all. 366 fields came out of `data-wiki.ts`, which is now 7020 lines.
- [x] 7.7 Update `catalog-source.test.ts`: drop the weapon-vocabulary invariant, move the counts to the refreshed numbers, and add an invariant that every modifier subject is in a known vocabulary Counts moved to 850 items, 3761 drops, 441 icons, 25 skills and 267 references. Three assertions changed meaning rather than number, and each says why in place: the fractional chance is back to the original scrape's 37.7861 now that the group gate is folded in; epic no longer implies grade `A`, because this server's three epics are levels 56-58; and the ambiguous items are 63 rather than 6, which is the refreshed drop tables spreading equipment across bands, so the count is the tripwire and the six the operator reviewed are still named. The new invariant reads its vocabularies out of the component schemas rather than restating them, so it fails on a word the database would refuse. 206 backend tests pass.
- [x] 7.8 Update the frontend type and the boss list, which read the removed relations; verify the list renders and the frontend lints clean Verified: `next build` renders `/raid-bosses`, and `check-types` and `check` are clean across both apps. No component read the four fields — they reached the type and the `populate` block and went no further — so the change is the type, the query and a `Skill` type mirroring the content type. The six weapon labels moved here as `WEAPON_LABELS`, which is where the element and statistic labels already live.
- [x] 7.9 Complete the spec delta, including the REMOVED requirements with their reasons and migrations; verify `openspec validate refresh-catalogue-from-game-wiki --strict` passes A second REMOVED requirement was needed and is now there. "Source defects are corrected on the way in" described repairing a placeholder location the earlier scrape produced; the reference names Eastern Border Outpost and Lachik Habitat itself, so the repair had no subject left and was removed with the table it read. The delta is merged into `openspec/specs/raid-boss-catalog/spec.md` at archive time, not here.

## 8. Bringing the catalogue up to date

- [x] 8.1 Back up the database before anything is applied; verify the dump's row counts against the live tables `apps/backend/backups/rb_timer-before-wiki-reseed-20260919-141639.sql`, 1.9 MB, `pg_dump --clean --if-exists --no-owner --no-privileges`; `.gitignore`'s `*.sql` covers it. Counted inside the dump against the live tables and every one agrees: 158 bosses, 896 items, 3611 drops, 66 locations, 96 avatars, 678 files, 6 weapon types. The rows the wipe must not touch are in it too — 114 users, 5 roles, 122 permissions, 43 donations, 71 legacy timer rows, 1 community, 1 admin. Restore with `psql -d rb_timer -f <dump>`; a restore does not remove the image files under `public/uploads`, which are simply left unreferenced.
- [x] 8.2 Run the profile and map refreshes once to bring every field current, then the drop refresh; verify the reports are empty on an immediate second run of each All three report nothing: drops 0 renamed / 0 added / 0 removed / 0 changed, profile 0 on level, race, statistics, respawn, soul crystal and skills, map 0 moved and 0 sharpened. Worth stating plainly what that does and does not prove: the passes read a disk cache that never expires, so this says the mock agrees with the source as it was fetched today, not as it stands this minute. Re-reading it live would be about 320 requests against a host that has throttled us once; clearing `.tmp/wiki-cache` is how to force that when a patch lands.
- [x] 8.3 Empty the 23 catalogue tables before seeding — `raid_bosses`, `items`, `boss_drops`, `locations`, `avatars`, `grades`, `weapon_types`, their link and component tables — and leave `files` alone so the seed reuses the uploaded media by name instead of writing a second copy of all 678. `bosses` is the deprecated respawn timer and is not part of the catalogue; users, donations and communities are not either. Wiping rather than pruning because the slug rule changed under 155 records and this catalogue has never been in production. Then seed, and verify the counts land Done. 1103 polymorphic media links went with the records; `files` kept all 678, and the seed reused 641 of them by name and uploaded 32 — the 23 skill icons, 8 item icons and the wiki map, all new to this change. Seeded in 26s: 158 bosses, 850 items, 3761 drops, 25 skills, 267 boss-to-skill links, 66 locations, 96 avatars, 6 grades. The five tables the new schema no longer defines needed no manual `DROP`: Strapi removed `weapon_types`, both affinity link tables and both `catalog.*` modifier component tables itself on the boot, leaving 77 tables. Untouched as intended: 71 legacy timer rows, 114 users, 43 donations.
- [x] 8.4 Run `prune:catalog` in report mode and read the list before deleting; verify the stranded pre-rename items appear and nothing still in use does The list is empty, and that is the right answer rather than a failed check: this task was written when the plan was to prune the old catalogue in place, and 8.3 wiped it instead, so nothing was left stranded to find. An empty report is now the evidence that the seed wrote exactly what the source states and not a row more.
- [x] 8.5 Run the prune for real, then seed again; verify counts are unchanged by the second seed Both run. The prune with `--delete` removed nothing, the second seed uploaded no images and reused all 673, and every count is identical to before: 158 bosses, 850 items, 3761 drops, 25 skills, 267 links, 66 locations, 96 avatars, 710 files, 1082 media links.
- [x] 8.6 Verify the corrections landed end to end: the epic levels, a boss whose drop line the server removed, and a renamed item reachable under its new name with its drops attached All three, read out of the live database and checked against the backup taken before any of it:
  - Epic levels: Core 58, Orfen 56, Queen Ant 56, where the backup has all three at 72. The four subclass bosses sit at 60 with a soul-crystal level of 11; the epics carry none, which is this server's rule and not a gap.
  - A removed drop line: `Atraiban — Zubei's Helmet Design`. The boss and the item both still exist and the line joining them is gone; Atraiban now states 25 drops. 345 of the backup's 3611 lines went this way, and 495 arrived.
  - A renamed item: `Blood Tornado` is gone and `Unidentified Blood Tornado` is reachable at `unidentified-blood-tornado`, graded A, with its icon and 4 drops attached.
  - And the shape this change was for: Gargoyle Lord Sirocco resolves through the new relation to five weapon modifiers across four skills — its own `Dagger/Rapier Resistance Lv. 3` reading `+3 level` beside the `Beast Lv. 3` racial trait reading `-10 percent` for the same weapon. Both kept, told apart by `origin`, which is exactly the case the old two-list shape could not express.

## 9. Grade comes from the source, not from a guess

Found after the seed, from an operator noticing a long list of items sitting at
`NG` that are plainly `C`. The visible symptom was 57 items the epic bosses had
made ambiguous; the cause underneath is that the reference states a grade on
every drop row, the parser reads it, and the catalogue throws it away in favour
of a rule that infers one from boss levels.

- [x] 9.1 Carry the grade the source states into `drops.ts` and `data-wiki-gamma.ts`; verify all 3761 rows carry one and that no item is stated at two different grades All 3761 carry one and 0 of the 850 items are stated twice: NG 2448, D 326, C 502, B 272, A 213. The refresh reports a grade change like any other value, so a rebalance that moves an item between grades arrives rather than landing in silence; that first run reported all 3761 rows, the file having held none, and the second reports zero.
- [x] 9.2 Take an item's grade from the source. Remove the name rule and the boss-agreement rule with it, along with `ambiguousItems`: with a grade stated for every item there is nothing left to be ambiguous about. Verify the 500 items whose grade the two rules disagree about now read as the source states, `Berserker Blade` at `C` among them Done. The distribution moves from 175/169/223/220/63 (A/B/C/D/NG) to 57/64/144/142/443, which is 437 fragments, recipes and scrolls no longer passed off as equipment and 57 pieces of real `C` gear no longer sitting at `NG`. `Scroll: Enchant Armor (D-Grade)` reads `NG`: the grade in its name is what it enchants.
- [x] 9.3 Take a boss's grade from the highest grade in its drop list, and remove the level bands. Measured across all 158: the two rules agree on 154, no boss's top grade rests on a single row, and no boss has an empty list. The four they disagree about are the subclass spirits, whose six drops are all `NG` because their reward is a subclass rather than equipment — they move to `NG`, and an operator who wants otherwise sets it by hand. Verify the other 154 are unchanged Verified: D 52, C 35, B 30, A 37 and the four spirits at NG, against D 52, C 35, B 34, A 37 before. The three epics stay `B` under both rules, by different routes — their best drops are `B`, and their levels are 56 to 58.
- [x] 9.4 Update the tests to the rule that replaced the bands, and add an invariant that every item's grade is one the source stated Four tests about the retired rules became two about the new one. The invariant reads `drops.ts` itself rather than the reader's output — the first draft compared the reader with itself and would have passed whatever it did. While in the file: the pair key in `pairs a boss with an item at most once` was a literal NUL, committed that way in an earlier change, which made git treat the whole file as binary. Replaced with a pipe, which a boss slug cannot contain either.
- [x] 9.5 Amend the spec delta: the grade requirement's derivation half is replaced A MODIFIED block turned out to be the wrong instrument and the validator said so: all four of the requirement's derivation scenarios describe rules that no longer exist, and a MODIFIED block may not drop a scenario. So it is a REMOVED with its reason and migration plus an ADDED under a name that says what the rule now is, which is the shape this change already used for the affinities.
- [x] 9.6 Re-run the drop refresh, reseed, and verify against the database: the items that were wrongly `NG`, the items that were wrongly graded equipment, and the four bosses that moved Read back out of the live database after reseeding: items NG 443, D 142, C 144, B 64, A 57 and bosses NG 4, D 52, C 35, B 30, A 37. `Berserker Blade`, `Akat Long Bow` and `Composite Armor` all read `C`, `Unidentified Blood Tornado` `A`, `Proof of Loyalty` and `Scroll: Enchant Armor (D-Grade)` `NG`. 208 backend tests, `check-types` and `check` clean.

## 10. Making the refresh survivable without notes

The passes work, but knowing how to run them lived in one conversation: the
order, the flags, and above all that the cache never expires, so the whole
sequence would cheerfully report "no differences" against last week's copy.

- [x] 10.1 Cache into a dated folder, `\.tmp/wiki-cache/<YYYY-MM-DD>/`, defaulting to today so a new day fetches a new copy by itself and `rm -rf` stops being part of the procedure. `--cache=<date>` takes an older one deliberately. Every pass states which folder it is using and how many pages are in it; when today's is empty and an older one exists it names the command to reuse it. Verify a second run the same day fetches nothing, and that the report names the date Done, and the day is the local one rather than UTC: west of Greenwich `toISOString` starts saying tomorrow partway through the evening, and these folder names are read by a person deciding whether a copy is today's. The existing 534 pages were moved into `2026-09-19/` rather than discarded.
- [x] 10.2 Remove `--fresh`. It bypassed the cache without filling it, so a `--fresh` profile run left `refresh:skills` reading pages from the previous copy — a trap that only fires on the pass that costs nothing to run. Dated folders replace it
- [x] 10.3 Record the date the source was read in the data files themselves, so the age of the data is visible in the file and not only in the cache folder. Have the seed report it `SOURCE_READ_ON` is an exported constant in each generated file rather than a comment, so the reader can pass it on. The reader reports the oldest of the three, because the catalogue is only as current as its stalest file, and reads it as text rather than evaluating it so a file written before this existed simply has none.
- [x] 10.4 Add `:write` siblings for every pass and `prune:catalog:delete`, so no flag has to be remembered, and end each report with the command that comes next; verify the chain leads from the first pass to the seed Verified end to end: drops -> profile -> skills -> map -> build and seed. The chain lives in one file rather than in five scripts, because a sequence written down in five places disagrees with itself within a month.
- [x] 10.5 Have the prune remove bosses the source no longer lists, after their drops and before the items. A boss left behind keeps its record with every drop pruned away, which reads exactly like a boss that drops nothing. Report rather than remove one whose grade was set by hand, as with items Removed between the drops and the items: a removed boss's drops are all among the stale ones, so they go first either way, and the items still need the surviving drops counted.
- [x] 10.6 Write the procedure down in `docs/CATALOGUE.md` and link it from the README: what the files are, what each pass costs, the order and why it is that order, and what the rate limiting will do if it is ignored `docs/CATALOGUE.md`, beside `MIGRATION.md`, which is the file in this repository for what cannot be recovered from the code. Linked from the README. The backend README is Strapi's own boilerplate and was left alone.
- [x] 10.7 Run the frontend e2e suite, which has not been run against the refreshed catalogue 10 passed, the three catalogue ones among them: a visitor with no account reads the list, resting on a row shows what it drops, and the gate still refuses. Playwright starts and stops both dev servers itself; nothing was left listening on 1337 or 3000 afterwards.

## 11. The maps become reachable

The seed uploads both world maps and attaches them to nothing, so the only way
to the image is a URL carrying the random suffix Strapi generates at upload
time — which changes on the next seed. The file is public; it is the address
that nobody can discover. "Replaceable by an operator without a deployment",
which the imagery requirement already asks for, is not true of them today.

- [x] 11.1 Add a `map` single type holding the catalogue's own imagery: the hand-assembled world map that `mapX`/`mapY` are measured on, and the source's map that `wikiX`/`wikiY` are pixels of. No separate field for the 3004-pixel basis — Strapi stores a media file's width, so the basis travels with the image and cannot disagree with it The fields are `map` and `wikiMap`, named for the coordinates that are measured on each rather than for what the picture shows, so `map` pairs with `mapX`/`mapY` and `wikiMap` with `wikiX`/`wikiY`. The type is named for what it holds and only maps go in it; anything else the catalogue needs a picture of gets a type of its own. Two constraints came out of this that the documentation does not state: a content type's folder name must equal its `singularName`, and singular and plural names share one namespace, so they cannot match — which is why a type answering at `/api/maps` is not possible without inventing a plural. `/api/map` is the shape Strapi uses for its own single types. `WIKI_MAP_SIZE` went with this: it recorded the 3004 beside the image, which is the mistake the field description argues against, and nothing read it.
- [x] 11.2 Seed both maps into it, reusing the files already uploaded rather than adding a second copy; verify a re-seed leaves the file count where it was and the record pointing at the same two files Verified: 710 files before and after, the record holding `map` at 3072x4096 and `wikiMap` at 3004x3004. This write had to go around the Document Service, which is the only such write in the seed: writing a media field through it leaves a populate of the response running unawaited, and this record is written last, so `app.destroy()` closed the pool under it and the seed hung sixty seconds and exited non-zero every run with the data correctly written. The documented `populate` and `fields` options do not suppress it; both were tried.
- [x] 11.3 Grant the public role read on it, as with the rest of the catalogue `find` only — a single type has no `findOne`.
- [x] 11.4 Give the frontend a type and a reader for it; verify `check-types` and the build `Maps` and `getMaps()`, reading the single type in one request, so a caller writes `const maps = await getMaps()`. Both apps type-check, lint and build; 209 backend tests pass.
- [x] 11.5 Amend the spec delta: the imagery requirement already names the world map, but nothing makes it addressable A MODIFIED block: the requirement's existing three scenarios are untouched and it gains the rule that an image belonging to the catalogue as a whole is still reachable through it, and is not addressed by a stored file name or URL.

## 1. Shared `packages/typescript-config` package

- [x] 1.1 Create `packages/typescript-config/package.json` (`name: "@rb-timer/typescript-config"`, `private: true`, no `main`/build step needed - plain JSON presets) and verify `pnpm -w ls -r` lists it as a workspace package.
- [x] 1.2 Create `packages/typescript-config/base.json` with only the runtime-independent options identified in design.md's Decisions (`skipLibCheck`, `esModuleInterop`, `resolveJsonModule`, `forceConsistentCasingInFileNames`, `incremental`, `noFallthroughCasesInSwitch`, `allowUnusedLabels: false`, `allowUnreachableCode: false`) and verify it contains no `module`/`target`/`strict`/`moduleResolution` keys.
- [x] 1.3 Create `packages/typescript-config/nextjs.json` extending `./base.json`, carrying every option currently inline in `apps/frontend/tsconfig.json` that isn't already in `base.json` (`target: ES2017`, `lib`, `module: preserve`, `moduleResolution: bundler`, `strict: true`, `noUnusedLocals`, `noUnusedParameters`, `noImplicitReturns`, `noUncheckedIndexedAccess`, `noImplicitOverride`, `verbatimModuleSyntax: true`) and verify with `tsc --showConfig -p packages/typescript-config/nextjs.json` that the resolved config matches today's `apps/frontend/tsconfig.json` compiler options exactly (aside from `include`/`exclude`/`plugins`, which stay app-local).
- [x] 1.4 Create `packages/typescript-config/strapi-server.json` extending `@strapi/typescript-utils/tsconfigs/server` plus `./base.json`, with `strict: false` explicit (matching Strapi's own preset) and verify `tsc --showConfig` resolves `module: CommonJS`, `target: ES2019`, `strict: false`. (Also added `@strapi/typescript-utils` as this package's own `dependencies` entry - its `extends` needs it resolvable from `packages/typescript-config/`, the same pnpm-hoisting gap task 4.1 fixes for `apps/backend`.)

## 2. Workspace wiring

- [x] 2.1 Add `packages/typescript-config` as a new literal entry in `pnpm-workspace.yaml` and verify `pnpm install` links it without touching unrelated lockfile entries (`git diff pnpm-lock.yaml` shows only the new package).
- [x] 2.2 Add `"@rb-timer/typescript-config": "workspace:*"` to `devDependencies` in both `apps/frontend/package.json` and `apps/backend/package.json`, and verify `apps/frontend/node_modules/@rb-timer/typescript-config` and `apps/backend/node_modules/@rb-timer/typescript-config` both exist as symlinks after `pnpm install`.

## 3. Frontend tsconfig cutover

- [x] 3.1 Rewrite `apps/frontend/tsconfig.json` to `"extends": "@rb-timer/typescript-config/nextjs.json"`, keeping only the Next-specific `include`/`exclude`/`plugins` blocks locally, and verify `pnpm --filter frontend exec tsc --noEmit` produces the same error set as before the change (empty, matching current clean baseline).
- [x] 3.2 Run `pnpm --filter frontend build` and verify it completes with no new type errors or warnings compared to the pre-change baseline.

## 4. Backend dependencies

- [x] 4.1 Add `@strapi/typescript-utils` pinned to the exact installed `@strapi/strapi` version (`5.52.2`), `typescript` pinned to `6.0.3` (matching frontend), and `@types/node` (matching the `engines` range `>=20 <=22`) as `devDependencies` in `apps/backend/package.json`, then run `pnpm install` and verify `apps/backend/node_modules/@strapi/typescript-utils` now exists (resolving the pnpm-hoisting gap found during exploration).

## 5. Backend tsconfig files

- [x] 5.1 Create `apps/backend/tsconfig.json` extending `@rb-timer/typescript-config/strapi-server.json`, adding `outDir: dist`, `rootDir: .`, `allowJs: true`, and Strapi's recommended `include`/`exclude` (excluding `node_modules/`, `build/`, `dist/`, `.cache/`, `.tmp/`, `src/admin/`, test files, `src/plugins/**`), and verify `tsc --showConfig -p apps/backend/tsconfig.json` resolves cleanly with no errors. Also fixed `tsBuildInfoFile`: Strapi's own `server.json` preset sets it to `./.tsbuildinfo`, which resolves relative to *that* file's location (deep in `node_modules/.pnpm/...`) per TS's extends-relative-path rule, not to this project - overrode it locally to `./.tsbuildinfo` relative to `apps/backend/`. Added `dist` and `.tsbuildinfo` to `apps/backend/.gitignore` (neither was covered by the existing bare `build` entry).
- [x] 5.2 Create `apps/backend/src/admin/tsconfig.json` extending `@strapi/typescript-utils/tsconfigs/admin` directly (per design.md - not routed through the shared package), and verify it resolves with `tsc --showConfig -p apps/backend/src/admin/tsconfig.json`. (`--showConfig` errors with `TS18003: No inputs were found` since `src/admin/` has no real `.tsx` entrypoint, only `.example.js` templates - this confirms the `extends` chain itself resolved, since an unresolvable extends fails differently/earlier.)

## 6. Backend source conversion to TypeScript

- [x] 6.1 Convert all six `apps/backend/config/*.js` files (`admin`, `api`, `database`, `middlewares`, `plugins`, `server`) to `.ts`, fixing `database.ts`'s SQLite path to use a second `'..'` (compiled output lands one directory deeper under `dist/config/`), and verify each file type-checks with no `any`-implicit errors. All six type-check clean under `tsc --noEmit`.
- [x] 6.2 Convert `apps/backend/src/index.js` to `src/index.ts` (register/bootstrap + socket.io setup), and verify it type-checks under `strict: false` with the `strapi.io = io` assignment intact. Left the `{ strapi }` destructured params untyped (implicit `any`, allowed under `strict: false`) rather than annotating with `Core.Strapi` - that type has no `io` property and no index signature, so the existing `strapi.io = io` assignment would otherwise fail to compile regardless of `strict`.
- [x] 6.3 Convert `apps/backend/src/extensions/users-permissions/strapi-server.js` to `.ts`, and verify it type-checks and the custom `PUT /users/me` route registration (`plugin.routes["content-api"].routes.unshift(...)`) is unchanged in behavior.
  - Discovered and fixed a **pre-existing bug**, confirmed unrelated to this migration (reproduced against the original, unconverted `.js` file at its real path via plain `node -e "require(...)"`): the file did `require('lodash')` / `require('@strapi/utils')`, but neither was ever a real `apps/backend` dependency, only transitive ones invisible under pnpm's strict layout - so this `require` had always failed at runtime for anyone hitting `/users/me` or the overridden `find`/`findOne`/`update`. Researched Strapi 5's docs (`plugins-development/plugins-extension`, `features/users-permissions`) to confirm: the `src/extensions/<plugin>/strapi-server.ts` mechanism is still the current, documented, non-deprecated way to do this (no native replacement exists for custom update-endpoint logic); the one documented caveat (factory-based controllers needing a wrapper, not direct assignment) applies only to the `auth` controller, which this file doesn't touch. Fixed by adding `lodash` (`^4.18.1`) and `@strapi/utils` (`5.52.2`) as real `dependencies`, plus `@types/lodash` (`^4.17.25`) as a `devDependency`.
  - That install surfaced a second, genuine `pnpm-lock.yaml` inconsistency (self-inflicted by an earlier bad `@types/lodash` version spec that aborted an install mid-resolution): the lockfile's `apps/backend` importer entry pointed `@strapi/utils` at a bare `version: 5.52.2` with no matching snapshot in `pnpm-lock.yaml`'s `snapshots:` section (only a peer-keyed `5.52.2(ts-toolbelt@9.6.0)` snapshot existed, already used transitively via `@strapi/strapi`) - producing a broken symlink (`apps/backend/node_modules/@strapi/utils` pointed at a `.pnpm` store path that was never materialized). A full `rm -rf node_modules` + reinstall reproduced the identical broken state, proving it was baked into the lockfile, not just stale `node_modules`. Fixed by pointing the importer's `version` at the existing peer-keyed snapshot (`5.52.2(ts-toolbelt@9.6.0)`), matching the exact pattern already used elsewhere in the same lockfile (e.g. `@strapi/plugin-users-permissions`'s own importer entry) - confirmed via `readlink` that the symlink now resolves to a real, existing path.
  - Also fixed two unrelated, purely mechanical `tsc` errors with no behavior change: an unused second `ctx` argument passed to the 1-parameter `sanitizeOutput` function (silently ignored in JS, flagged by TS arity checking), and an `unknown`-typed `advancedConfigs.unique_email` property access (from `strapi.store(...).get()`'s generic return type) needing a cast.
  - Re-verified `pnpm --filter frontend build` stays clean after the lockfile repair (not just `apps/backend`'s own typecheck), since the repair touched the shared root lockfile.
- [x] 6.4 Convert all 15 content-type files (`boss`, `collection`, `donation`, `effect`, `item` x `controllers`, `services`, `routes`) to `.ts`, switching to the typed factory form, and verify each resolves the correct generated type from `types/generated/contentTypes.d.ts` with no type errors. Used `factories.createCoreController("api::boss.boss")` (etc.) without an explicit `<'api::boss.boss'>` type argument - TS infers it from the string literal identically; matches Strapi's own generated-template convention. All 15 type-check clean.
- [x] 6.5 Verify no `.js` files remain under `apps/backend/src/` or `apps/backend/config/` (excluding `src/admin/*.example.js`, which stay untouched per design.md's Non-Goals) via `find apps/backend/src apps/backend/config -name "*.js" -not -path "*/admin/*"` returning empty. Confirmed empty.

## 7. Type generation

- [x] 7.1 Run `pnpm --filter backend strapi ts:generate-types` and diff the regenerated `types/generated/contentTypes.d.ts`/`components.d.ts` against the pre-change versions, verifying only expected/no changes. `diff` against copies saved before conversion: zero changes in either file. (Note: these files were already untracked/gitignored before this change - `git ls-files` confirms they've never been committed, so this diff was purely a local sanity check, not a git-tracked concern.)

## 8. Backend build and runtime verification

- [x] 8.1 Run `pnpm --filter backend build` and verify it completes, producing compiled server output under `dist/` and the admin bundle, with no `tsc` errors. Clean: "Compiling TS" (956ms), "Building admin panel" (12.7s), no errors.
  - Blocked initially by a design.md-level issue: `ignoreDeprecations: "6.0"` (added in task 5.1 to satisfy `apps/backend`'s own pinned `typescript@6.0.3`) made `strapi build` itself fail with `TS5103: Invalid value for '--ignoreDeprecations'` - because Strapi's build pipeline doesn't use the project's own pinned `typescript` at all; `@strapi/typescript-utils@5.52.2` bundles and invokes its own (`5.9.3`), which doesn't recognize `"6.0"` as a valid value. Tested both directions directly: no single `ignoreDeprecations` value satisfies both 5.9.3 and 6.0.3 at once, and switching `moduleResolution` away from Strapi's own `"Node"` to a non-deprecated equivalent (`nodenext`) cascades into requiring `module: "NodeNext"` too - a much larger deviation from Strapi's own server preset than intended.
  - **Correction to design.md's Decisions**: re-pinned `apps/backend`'s own `typescript` devDependency to `5.9.3` (matching what Strapi's build pipeline actually runs, instead of `6.0.3` to match the frontend) and removed `ignoreDeprecations` entirely (no longer needed - 5.9.3 predates the deprecation-as-error behavior). The original "one compiler version across the monorepo" rationale assumed our own pin controls the backend's real compile step; it doesn't, so frontend (whose real build *does* use its own pinned `typescript`, confirmed via `next build`'s own "Running TypeScript" step) and backend now each pin to whatever their own actual build tool uses - the more correct alignment, not a compromise.
- [x] 8.2 Run `pnpm --filter backend dev` and verify Strapi boots against the existing `apps/backend/.tmp/data.db` (not a new empty database under `dist/`), confirming the SQLite path fix from 6.1 actually took effect. Booted clean ("Strapi started successfully", `Database name: .tmp/data.db`); MD5 checksum of `.tmp/data.db` identical before and after boot (`ac1b8d564b1a6da6e9fce9df4d48858d`), confirming it opened the existing file, not a fresh one under `dist/`.
- [x] 8.3 Manually exercise the socket.io flow (client connects, `join`/`auth`/`reset` events) and at least one REST content-type endpoint (e.g. `GET /api/bosses`) against the running dev server, verifying identical behavior to the pre-migration JS backend. Socket.io: connected a real `socket.io-client`, emitted `join`, received the expected `socketUsers` broadcast back - full round-trip through the converted `src/index.ts` bootstrap logic. REST: `GET /api/bosses` returned `403 Forbidden` (`ForbiddenError`) - the correct, expected response for an unauthenticated request against a content-type with no public-role permissions granted (a DB/config matter, unrelated to this migration); confirms the route/controller/service/permissions-middleware pipeline is reachable and functioning, not crashing (would have been a 500 or connection failure if the TS conversion broke something). Did not have test credentials on hand to also exercise the `/users/me` extension endpoint live; its fix is verified at the type/dependency-resolution level (task 6.3) but not via a live authenticated request.

## 9. Final cross-app verification

- [x] 9.1 Re-run `pnpm --filter frontend build` and `pnpm --filter backend build` together from a clean install (`pnpm install` at repo root) and verify both succeed, closing the loop on the shared `packages/typescript-config` package serving both apps. Wiped all `node_modules` (root + all 3 workspace packages), `apps/backend/dist`, `apps/backend/.tsbuildinfo`, and `apps/frontend/.next`; fresh `pnpm install` correctly re-linked all previously-fixed symlinks (`@rb-timer/typescript-config`, `@strapi/typescript-utils`, `@strapi/utils`, `lodash`) with no manual intervention; both `pnpm --filter frontend build` and `pnpm --filter backend build` succeeded clean.

## 10. Post-completion follow-up: unify `typescript` version and adopt pnpm catalog

Requested after the change above was reported complete. Supersedes part of task 8.1's correction (see design.md's updated Decisions).

- [x] 10.1 Checked whether frontend could go down to `5.9.3` instead of backend going up to `6.0.3` (the direction not checked in task 8.1). Confirmed free of constraints: `eslint-config-next`'s own peer range is `"typescript": ">=3.3.1"` (no `6.0` floor), and the archived `modernize-frontend-tsconfig` change's own design.md states the `5.9.3`→`6.0.3` bump was "the same compiler engine ... a low-risk version bump with no behavioral surprises," i.e. modernization, not a requirement.
- [x] 10.2 Re-pinned `apps/frontend/package.json`'s `typescript` from `6.0.3` back to `5.9.3` (via catalog, see 10.3), unifying the whole monorepo on one TypeScript version.
- [x] 10.3 Added a default `catalog:` to `pnpm-workspace.yaml` with `typescript: 5.9.3`, `@types/node: 22.20.1`, `@strapi/typescript-utils: 5.52.2`; updated `apps/frontend/package.json`, `apps/backend/package.json`, and `packages/typescript-config/package.json` to reference `"catalog:"` for these three instead of repeating literal versions. Deliberately did **not** catalog `react`/`styled-components` (genuinely different per app on purpose) or any single-consumer dependency (no benefit from catalog indirection).
- [x] 10.4 Verified via a full from-scratch reinstall (`rm -rf` all `node_modules` + `pnpm install`): both apps resolve `typescript@5.9.3` (`tsc --version`), the catalog section resolves cleanly in `pnpm-lock.yaml`, and the previously-fixed `@strapi/utils`/`lodash` symlinks remain correct with no manual lockfile intervention needed this time. `tsc --noEmit` clean on both apps; `pnpm --filter frontend build` and `pnpm --filter backend build` both clean.

## 11. Post-completion follow-up: remove `lodash` from `apps/backend`

Requested after noticing `lodash` was pulled in for a single file, while frontend already uses `ramda`.

- [x] 11.1 Confirmed `lodash` is used nowhere in `apps/backend` except `strapi-server.ts`'s six `_.has(ctx.request.body, "<key>")` calls (`grep -rln lodash src config`), and confirmed via diff against `@strapi/plugin-users-permissions/dist/server/controllers/user.js` that the file is a near-verbatim copy of Strapi's own controller - explaining why `lodash` was there in the first place (see design.md's updated Decisions).
- [x] 11.2 Replaced all six `_.has(...)` calls with a local `hasOwn(object, key) => Object.prototype.hasOwnProperty.call(object, key)` helper and removed the `import _ from "lodash"` line. Deliberately did not swap to `ramda` instead - see design.md for why (new dependency either way, reversed argument order, no gain over the native one-liner).
- [x] 11.3 Removed `lodash` and `@types/lodash` from `apps/backend/package.json`. Verified via `pnpm install` that neither is linked into `apps/backend/node_modules` any more (still present transitively via `@strapi/strapi`, just not declared or directly reachable by our own code).
- [x] 11.4 Verified via a full from-scratch reinstall: `tsc --noEmit` clean, `pnpm --filter backend build` clean, `pnpm --filter frontend build` clean. Confirms the earlier lesson (removing an undeclared-but-imported dependency reintroduces the `TS2307` error) doesn't apply here, since the `import` statement itself was removed along with the dependency, not left dangling.

## 1. Dependencies and config

- [x] 1.1 Add `@playwright/test` to `frontend/package.json` devDependencies and run `pnpm install`, verifying it installs cleanly — pinned to `1.62.1` (exact, matching this project's version-pinning convention)
- [x] 1.2 Create `frontend/playwright.config.ts` with the `webServer` array (backend on `:1337`, frontend on `:3000`, `reuseExistingServer: !process.env.CI`) and verify `npx playwright test --list` runs without config errors — used `pnpm exec playwright` (not `npx`) to respect the pinned local version; added `channel: 'chrome'` to use the already-installed system Chrome instead of downloading Playwright's own bundled Chromium (design.md updated with this decision); `--list` correctly reports "No tests found" (config loads fine, no spec files exist yet)
- [x] 1.3 Gitignore the `storageState` output path (e.g. `frontend/playwright/.auth/`) and verify `git status` shows it ignored after a test run creates it — used `frontend/e2e/.auth/` (colocated with the test suite); also proactively ignored `test-results`/`playwright-report`/`blob-report` generated output dirs. Verified with a dummy file at the exact path — `git status --short` shows nothing

## 2. Fixture role-upgrade helper

- [x] 2.1 Write a small Node script (using `better-sqlite3`) that, given the fixture email, looks up the `officer`-typed role id from `up_roles`, checks whether the user is already linked to it in `up_users_role_lnk`, and if not, deletes any existing role link for that user and inserts the officer link — with retry-with-backoff around the write in case `backend`'s own connection holds a lock — **placed in `backend/scripts/e2e-fixture-role.js` (invoked as a subprocess via a new `pnpm run e2e:fixture-role <email>` script), not as a new frontend dependency**: `better-sqlite3` is already a backend dependency and backend owns the database, so this avoids installing a second copy of a native module and respects this repo's established backend/frontend independence (per README). Verified `Atomics.wait`-based retry actually works on Node's main thread (tested directly) before relying on it in the retry loop
- [x] 2.2 Verify the script is idempotent: running it twice in a row against a user already on the officer role makes no further writes (confirm via a direct `sqlite3` query before/after) — tested against a temporary throwaway user row (id 139), confirmed the `up_users_role_lnk` row's own `id` stayed identical (115) across the second run, then deleted the temporary row entirely; real user count back to 109

## 3. Auth setup project

- [x] 3.1 Add a Playwright `setup` project/spec that: navigates to `/login`, fills the real login form with the fixture credentials, and on failure navigates to `/register` and fills the real registration form instead, then logs in — **revised locator strategy mid-implementation, at the user's direction**: form fields are located via a new centralized `data-testid` registry (`frontend/constants/test-ids.ts`, imported by both app components and tests) rather than `getByLabel`, since Playwright's own docs confirm test ids survive text/copy changes that labels don't — the right tradeoff given this app is expected to churn. Added a `MayHaveTestId` shared prop type (`frontend/types/test-id.types.ts`) and threaded `data-testid` through the one component that didn't already forward extra props (`Input`); `Button` needed no change (styled-components + antd already forward it). Kept one `getByLabel` check as a supplementary a11y sanity assertion, per the user's suggestion. Verified end-to-end twice: once exercising the register branch (fresh fixture), once exercising the login-first branch (fixture already existed) — both passed
- [x] 3.2 Call the role-upgrade helper from the setup spec after a successful login/registration, and verify (via the running app, e.g. checking a management-only element becomes visible) that the fixture account actually has officer-level access afterward — verified via the new `TEST_IDS.profileManagement.deleteButton` testid (officer-only "Удалить" button), confirmed visible after navigating to `/profile/management`
- [x] 3.3 Save `page.context().storageState()` to the gitignored path from 1.3, and configure the other test projects to depend on `setup` and load that `storageState` — confirmed the file is written at `frontend/e2e/.auth/fixture-user.json` and the `chromium` project's `dependencies: ['setup']` + `storageState` wiring works (verified in the two full runs above)

## 4. Smoke-test specs

- [x] 4.1 Add a smoke spec for `/` (boss list): loads, a key heading/element is visible, zero uncaught console errors — added `e2e/fixtures/test.ts`, an auto-fixture extending Playwright's `test` that fails on any console `error` or uncaught `pageerror`, reused by all 4 specs; located via `TEST_IDS.bossList.table`
- [x] 4.2 Add a smoke spec for `/profile/collections`: loads while authenticated as the fixture (officer) account, a key element is visible, zero uncaught console errors — no interaction with any collection item — added one new testid (`TEST_IDS.profileCollections.effectsBlock`) since this screen had no stable existing marker
- [x] 4.3 Add a smoke spec for `/profile/management`: same shape as 4.2, scoped to the management tab — reuses `TEST_IDS.profileManagement.deleteButton`, doubling as confirmation the officer role still holds
- [x] 4.4 Add a smoke spec for `/users`: same shape, no interaction with any real user row

## 5. Wiring and verification

- [x] 5.1 Add `frontend/package.json`'s `test:e2e` script (`playwright test`) and verify `pnpm test:e2e` runs the full suite green from a cold checkout (no manually-started dev servers), confirming `webServer` boots both apps and tears them down after — also added `test:e2e:ui` (`playwright test --ui`) at the user's request, for interactive manual runs. Verified: 5/5 passed cold (both servers auto-launched via `webServer`), both ports confirmed down afterward (auto-teardown), real user count stable at 110 (109 original + 1 fixture)
- [x] 5.2 Verify `pnpm test:e2e` also runs green with `backend`/`frontend` already running manually in separate terminals (per the README's existing workflow), confirming `reuseExistingServer` leaves those processes untouched both before and after the run — confirmed by PID: started both manually (backend pid 36413, frontend pid 36419), ran the full suite (no `[WebServer]` boot logs, ~8s vs ~18s cold), then confirmed the exact same PIDs were still listening on `:1337`/`:3000` afterward — Playwright neither restarted nor killed them
- [x] 5.3 Confirm no changes were made to any of the 109 pre-existing real user rows in `up_users`/`up_users_role_lnk` after a full suite run (row count and existing rows' `updated_at` unchanged) — confirmed via `date(updated_at/1000, 'unixepoch')`: none of the 109 real users have today's date (2026-09-02), only the fixture account does, despite multiple suite runs today; total row count stable at 110 (109 real + 1 fixture)
- [x] 5.4 Added `test:e2e:headed` (`playwright test --headed`) at the user's request, after clarifying that `--ui` doesn't auto-run tests or show a live browser (it's a post-run trace/timeline viewer you drive manually) — `--headed` is the flag that opens a real, visible Chrome window during the run
- [x] 5.5 Added `SLOWMO` env-var support (`use.launchOptions.slowMo` in `playwright.config.ts`) so `--headed` runs can be paced for a human to follow — no CLI flag exists for this (checked `playwright test --help`), config is the only way; defaults to 0 (no effect) so normal headless runs are unaffected. Baked `SLOWMO=1000` directly into the `test:e2e:headed` script at the user's request, so no env var needs to be typed manually (value later tuned from an initial 500 to 1000 by the user directly)
